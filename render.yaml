services:
  - type: web
    name: profile-analyzer
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: APIFY_API_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
    autoDeploy: true